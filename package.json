{
  "name": "thumbnail-counter-users",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "Responsive editable thumbnail counter with private user accounts and cross-device synchronization on Netlify.",
  "scripts": {
    "dev": "netlify dev"
  },
  "dependencies": {
    "@netlify/blobs": "10.7.9",
    "@netlify/identity": "1.2.0"
  }
}
